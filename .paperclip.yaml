schema: paperclip/v1
agents:
  clara-muse:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4o
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: optional
        OPENAI_API_KEY:
          kind: config
          requirement: optional
  elias-redstone:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4o
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: optional
        OPENAI_API_KEY:
          kind: config
          requirement: optional
  marcus-christensen:
    adapter:
      type: opencode_local
      config:
        model: openai/code-reviewer
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: optional
        OPENAI_API_KEY:
          kind: config
          requirement: optional
  niko-relay:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: optional
        OPENAI_API_KEY:
          kind: config
          requirement: optional
  viktor-forge:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4-turbo
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: optional
        OPENAI_API_KEY:
          kind: config
          requirement: optional
