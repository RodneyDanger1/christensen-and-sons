schema: paperclip/v1
agents:
  clara-muse:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4o
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: required
        OPENAI_API_KEY:
          kind: secret
          requirement: required
  elias-redstone:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4o
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: required
        OPENAI_API_KEY:
          kind: secret
          requirement: required
  marcus-christensen:
    adapter:
      type: opencode_local
      config:
        model: openai/code-reviewer
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: required
        OPENAI_API_KEY:
          kind: secret
          requirement: required
  niko-relay:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: required
        OPENAI_API_KEY:
          kind: secret
          requirement: required
  viktor-forge:
    adapter:
      type: opencode_local
      config:
        model: openai/gpt-4-turbo
    inputs:
      env:
        OPENAI_BASE_URL:
          kind: config
          requirement: required
        OPENAI_API_KEY:
          kind: secret
          requirement: required
